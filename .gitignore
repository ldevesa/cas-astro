# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# backups locales (versiones alternativas para mostrar, no se deployan)
_backups/
public/_*

# migración a Sanity: snapshots crudos y datos transformados son regenerables
# (npm run extract / transform dentro de migration/); solo se versionan los
# reportes de revisión humana (matching, incidencias, import)
migration/extracted/
migration/transformed/
migration/reports/asset-map.json
